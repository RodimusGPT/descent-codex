---
part: 4
title: "Inference: hardware"
question: How does the silicon run it?
accent: hardware
---

import DeeperBlock from "../../components/scroll/DeeperBlock";
import ScrollScene from "../../components/scroll/ScrollScene";
import Token from "../../components/scroll/Token";

# Inference: hardware

The hardware layer connects the software loop to tensor cores, SRAM, and high-bandwidth
memory. The path of a <Token text="token" /> becomes a path through matrix tiles and
memory traffic.

<ScrollScene
  client:load
  title="Hardware placeholder"
  steps={[
    {
      id: "gpu",
      narration: "The GPU runs many matrix operations across streaming multiprocessors.",
      visualLabel: "GPU floorplan placeholder.",
    },
    {
      id: "memory",
      narration: "The memory hierarchy decides whether work waits on bytes or math.",
      visualLabel: "Memory pyramid placeholder.",
    },
    {
      id: "roofline",
      narration: "Roofline analysis ties arithmetic intensity to the bottleneck.",
      visualLabel: "Roofline arrives in M7.",
    },
  ]}
/>

<DeeperBlock summary="Go deeper">
  Later milestones will cover GEMM tiling, FlashAttention, rooflines, and model
  parallelism without making 3D a requirement.
</DeeperBlock>
